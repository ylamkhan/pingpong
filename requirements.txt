Django>=4.2  # The latest stable version of Django
djangorestframework>=3.15  # Django REST Framework for building APIs
djangorestframework-simplejwt>=5.2  # For JWT authentication
argon2-cffi>=21.3.0  # For secure password hashing (optional but recommended)
gunicorn>=20.1.0  # Production WSGI HTTP Server (optional for production)
python-dotenv>=1.0.0  # For managing environment variables
psycopg2>=2.9.7  # PostgreSQL driver (optional, if using PostgreSQL as your database)
